<?xml version="1.0" encoding="UTF-8"?>
<!-- Complementary Modules for MATH 14                                     -->
<!-- Section: Parametrization of Curves                                    -->
<!--                                                                       -->
<!-- Ported from the MATH 13 notes (chapter "Parametrization"), sections    -->
<!-- "Parametric Equations", "Some Examples of Parametrizing Curves" and    -->
<!-- "Intersection Points versus Collision Points"; the projectile-motion   -->
<!-- section is deliberately omitted.  The closing subsection on curves in  -->
<!-- space is new.                                                         -->
<!--                                                                       -->
<!-- Figure conventions: plane curves are drawn with PreFigure (annotated   -->
<!-- for screen readers); space curves are drawn with TikZ/pgfplots, which  -->
<!-- needs the tikz + pgfplots preamble already present in main.ptx.        -->

<section xml:id="sec-parametrization">
  <title>Parametrization of Curves</title>

  <objectives>
    <ul>
      <li>
        <p>
          Describe the motion of a point in the plane with a pair of
          parametric equations <m>x=f(t)</m> and <m>y=g(t)</m>, and
          understand the parameter <m>t</m> as recording <em>when</em> the
          point visits each place on the curve.
        </p>
      </li>
      <li>
        <p>
          Sketch a parametric curve from its parametric equations, and indicate the
          direction of increasing <m>t</m> in which the curve is traced.
        </p>
      </li>
      <li>
        <p>
          Find parametric equations for common curves, such as lines,
          circles, and ellipses, and eliminate the parameter to recover a
          Cartesian equation when possible.
        </p>
      </li>
      <li>
        <p>
          Distinguish the <term>intersection points</term> of two parametric
          curves from their <term>collision points</term>, where two moving
          objects reach the same place at the same time.
        </p>
      </li>
      <li>
        <p>
          Write a space curve as a vector function
          <m>\mathbf{r}(t)</m>, and parametrize the curve in which two
          surfaces meet.
        </p>
      </li>
    </ul>
  </objectives>

  <introduction>
    <p>
      Imagine that you would like to describe the motion of an object whose
      motion is confined to a plane. If you know how its Cartesian
      coordinates, <m>x</m> and <m>y</m>, change as time passes, you will be
      able to draw a curve that represents the trajectory of its motion.
      Mathematically, we will need two equations to demonstrate how the
      <m>x</m><ndash/> and <m>y</m>-coordinates evolve in time <m>t</m>,
      which are known as the parametric equations. This section reviews that
      language and then extends it to curves in space, where it becomes the
      main tool for everything that follows.
    </p>
  </introduction>

  <!-- ================================================================= -->
  <subsection xml:id="subsec-parametric-equations">
    <title>Parametric Equations</title>

    <definition xml:id="def-parametric-equations">
      <title>Parametric Equations</title>
      <statement>
        <p>
          If <m>x</m> and <m>y</m> are given as functions of a third
          variable <m>t</m>, called a <term>parameter</term>, by

        <md>
          <mrow xml:id="eq-parametric-equations" number="yes">
            x = f(t), \qquad y = g(t),
          </mrow>
        </md>
        </p>
        <p>
          then the points <m>(x,y) = \left(f(t),\,g(t)\right)</m> trace out a
          curve in the plane, called a <term>parametric curve</term>. The
          equations <m>x=f(t)</m> and <m>y=g(t)</m> are the
          <term>parametric equations</term> of the curve.
        </p>
      </statement>
    </definition>

    <p>
      As <m>t</m> runs over its interval, the point
      <m>\left(f(t),g(t)\right)</m> moves, and the trail it leaves behind is
      the curve. The parameter therefore carries more information than the
      picture does: it also records <em>when</em> the moving point is at each
      place, as in <xref ref="fig-trajectory"/>.
    </p>

    <figure xml:id="fig-trajectory">
      <caption>
        The trajectory of a moving point whose coordinates are given by the
        parametric equations <m>x=f(t)</m>, <m>y=g(t)</m>. At the instant
        <m>t_0</m> the point is at
        <m>\left(f(t_0),g(t_0)\right)</m>.
      </caption>
      <image xml:id="image-trajectory" width="60%">
        <shortdescription>
          A curved trajectory in the plane with a marked point and dashed
          lines dropping to the two coordinate axes.
        </shortdescription>
        <description>
          <p>
            A smooth curve rises and falls across the first quadrant from a
            starting point A, reached at time t equals a, to an ending point
            B, reached at time t equals b. A single point on the curve is
            marked; dashed gray segments run from it straight down to the x
            axis, where the value is labeled f of t nought, and straight left
            to the y axis, where the value is labeled g of t nought. An arrow
            on the curve shows the direction of increasing t.
          </p>
        </description>
        <prefigure xmlns="https://prefigure.org" label="trajectory">
          <diagram dimensions="(420,320)" margins="5">
            <definition>c(t) = (t, 2 + sin(t))</definition>
            <definition>t0 = 2</definition>
            <definition>P = c(t0)</definition>
            <coordinates bbox="(-0.7,-0.6,5.9,4.2)">
              <grid-axes xlabel="x" ylabel="y"/>

              <group at="curve">
                <parametric-curve function="c" domain="(0,5)" stroke="blue"
                                  thickness="3" arrows="1"
                                  arrow-location="3.4"/>
                <label anchor="c(4.3)" alignment="se" offset="(6,-6)"
                       clear-background="yes">
                  <m>C</m>
                </label>
              </group>

              <group at="ends">
                <point p="c(0)" size="4" stroke="black" fill="black"/>
                <label anchor="c(0)" alignment="nw" offset="(-4,4)"
                       clear-background="yes">
                  <m>A,\ t=a</m>
                </label>
                <point p="c(5)" size="4" stroke="black" fill="black"/>
                <label anchor="c(5)" alignment="se" offset="(4,-4)"
                       clear-background="yes">
                  <m>B,\ t=b</m>
                </label>
              </group>

              <group at="readout">
                <line endpoints="(P, (t0,0))" stroke="gray" thickness="2"
                      dash="4 3"/>
                <line endpoints="(P, (0,2+sin(t0)))" stroke="gray"
                      thickness="2" dash="4 3"/>
                <point p="P" size="4.5" stroke="red" fill="red"/>
                <label anchor="P" alignment="ne" offset="(6,6)"
                       clear-background="yes">
                  <m>\left(f(t_0),g(t_0)\right)</m>
                </label>
                <label anchor="(t0,0)" alignment="s" offset="(0,-8)"
                       clear-background="yes">
                  <m>f(t_0)</m>
                </label>
                <label anchor="(0,2+sin(t0))" alignment="w" offset="(-8,0)"
                       clear-background="yes">
                  <m>g(t_0)</m>
                </label>
              </group>
            </coordinates>

            <annotations>
              <annotation ref="figure"
                          text="The trajectory of a point moving in the plane,
                                with its position at the instant t nought read
                                off on the two coordinate axes">
                <annotation ref="grid" text="The coordinate grid"/>
                <annotation ref="axes" text="The coordinate axes"/>
                <annotation ref="curve"
                            text="The trajectory C, traced in the direction of
                                  increasing t"
                            sonify="yes"/>
                <annotation ref="ends"
                            text="The starting point A at time t equals a and
                                  the ending point B at time t equals b"/>
                <annotation ref="readout"
                            text="The position of the point at the instant t
                                  nought, with dashed segments to the axes
                                  showing its coordinates f of t nought and g
                                  of t nought"/>
              </annotation>
            </annotations>
          </diagram>
        </prefigure>
      </image>
    </figure>

    <p>
      As an example, consider <m>x = t</m> and <m>y = t^2</m>. Let us
      calculate some of the points <m>(x,y)</m>.
    </p>

    <table xml:id="table-parabola-points">
      <title>Points on the curve <m>x=t</m>, <m>y=t^2</m></title>
      <tabular halign="center" top="minor" left="minor">
        <row header="yes" bottom="minor">
          <cell><m>t</m></cell>
          <cell><m>-3</m></cell>
          <cell><m>-2</m></cell>
          <cell><m>-1</m></cell>
          <cell><m>0</m></cell>
          <cell><m>1</m></cell>
          <cell><m>2</m></cell>
          <cell><m>3</m></cell>
        </row>
        <row>
          <cell><m>x</m></cell>
          <cell><m>-3</m></cell>
          <cell><m>-2</m></cell>
          <cell><m>-1</m></cell>
          <cell><m>0</m></cell>
          <cell><m>1</m></cell>
          <cell><m>2</m></cell>
          <cell><m>3</m></cell>
        </row>
        <row>
          <cell><m>y</m></cell>
          <cell><m>9</m></cell>
          <cell><m>4</m></cell>
          <cell><m>1</m></cell>
          <cell><m>0</m></cell>
          <cell><m>1</m></cell>
          <cell><m>4</m></cell>
          <cell><m>9</m></cell>
        </row>
      </tabular>
    </table>

    <p>
      Plotting these points and connecting them, we recognize the parabola
      <m>y = x^2</m>, traced from left to right as <m>t</m> increases from
      <m>-3</m> to <m>3</m>. See <xref ref="fig-parabola-parametric"/>.
    </p>

    <figure xml:id="fig-parabola-parametric">
      <caption>
        The curve <m>x = t</m>, <m>y = t^2</m> is the parabola
        <m>y = x^2</m>. The marked points correspond to the integer values
        <m>t = -3, \dots, 3</m> from
        <xref ref="table-parabola-points"/>, and the arrow shows the
        direction of increasing <m>t</m>.
      </caption>
      <image xml:id="image-parabola-parametric" width="60%">
        <shortdescription>
          The parabola y equals x squared with seven marked points and an
          arrow showing that it is traced from left to right.
        </shortdescription>
        <description>
          <p>
            The parabola y equals x squared drawn on a coordinate grid for x
            between minus three and three. Seven dots on the curve mark the
            points obtained from the integer parameter values t equals minus
            three through three, namely minus three comma nine, minus two
            comma four, minus one comma one, the origin, one comma one, two
            comma four, and three comma nine. An arrowhead partway up the
            right branch shows that the curve is traced from left to right as
            t increases.
          </p>
        </description>
        <prefigure xmlns="https://prefigure.org" label="parabola-parametric">
          <diagram dimensions="(400,380)" margins="5">
            <definition>f(t) = (t, t^2)</definition>
            <coordinates bbox="(-4.2,-1.4,4.2,10.4)">
              <grid-axes xlabel="x" ylabel="y"/>

              <group at="curve">
                <parametric-curve function="f" domain="(-3,3)" stroke="blue"
                                  thickness="3" arrows="1"
                                  arrow-location="2"/>
                <label anchor="f(2.6)" alignment="e" offset="(8,0)"
                       clear-background="yes">
                  <m>y=x^2</m>
                </label>
              </group>

              <group at="samples">
                <repeat parameter="k=0..6">
                  <point p="f(k-3)" size="4" stroke="red" fill="red"/>
                </repeat>
                <label anchor="f(-3)" alignment="nw" offset="(-4,4)"
                       clear-background="yes">
                  <m>t=-3</m>
                </label>
                <label anchor="f(0)" alignment="s" offset="(0,-8)"
                       clear-background="yes">
                  <m>t=0</m>
                </label>
                <label anchor="f(3)" alignment="ne" offset="(4,4)"
                       clear-background="yes">
                  <m>t=3</m>
                </label>
              </group>
            </coordinates>

            <annotations>
              <annotation ref="figure"
                          text="The parametric curve x equals t, y equals t
                                squared, which is the parabola y equals x
                                squared traced from left to right">
                <annotation ref="grid" text="The coordinate grid"/>
                <annotation ref="axes" text="The coordinate axes"/>
                <annotation ref="curve"
                            text="The parabola y equals x squared, traced in
                                  the direction of increasing t"
                            sonify="yes"/>
                <annotation ref="samples"
                            text="The seven points coming from the integer
                                  parameter values t equals minus three
                                  through three"/>
              </annotation>
            </annotations>
          </diagram>
        </prefigure>
      </image>
    </figure>

    <p>
      As our second example, consider the equation of the circle centered at
      <m>(0,0)</m> with unit radius,

    <md>
      <mrow xml:id="eq-unit-circle" number="yes">
        x^2 + y^2 = 1.
      </mrow>
    </md>
    </p>

    <p>
      Here, we would like to parametrize <m>x</m> and <m>y</m> in terms of a
      single parameter <m>t</m> in such a way that by varying the value of
      <m>t</m>, we can reconstruct the circle <xref ref="eq-unit-circle"/>.
      Remember that <m>\cos^2(t) + \sin^2(t) = 1</m>, so one way to achieve
      our goal is to write

    <md>
      <mrow xml:id="eq-unit-circle-param" number="yes">
        x = \cos(t), \qquad y = \sin(t),
      </mrow>
    </md>
    </p>

    <p>
      where <m>t \in [0, 2\pi)</m>. The parametrization is illustrated in
      <xref ref="fig-unit-circle-param"/>.
    </p>

    <figure xml:id="fig-unit-circle-param">
      <caption>
        Parametrizing the unit circle <m>x^2+y^2=1</m> by
        <m>x=\cos(t)</m>, <m>y=\sin(t)</m>, <m>t \in [0,2\pi)</m>. A generic
        point of the circle corresponds to the parameter value <m>t_0</m>.
      </caption>
      <image xml:id="image-unit-circle-param" width="60%">
        <shortdescription>
          The unit circle traced counterclockwise, with the four quarter
          points marked and one generic point at parameter value t nought.
        </shortdescription>
        <description>
          <p>
            The unit circle on a coordinate grid, traced counterclockwise as
            indicated by an arrowhead. Four dots mark the points reached at t
            equals zero, at one comma zero; t equals pi over two, at zero
            comma one; t equals pi, at minus one comma zero; and t equals
            three pi over two, at zero comma minus one. A fifth dot part way
            around the first quadrant marks a generic point cosine t nought,
            sine t nought, joined to the origin by a segment that makes the
            angle t nought with the positive x axis.
          </p>
        </description>
        <prefigure xmlns="https://prefigure.org" label="unit-circle-param">
          <diagram dimensions="(380,380)" margins="5">
            <definition>c(t) = (cos(t), sin(t))</definition>
            <definition>t0 = 0.9</definition>
            <coordinates bbox="(-1.6,-1.6,1.6,1.6)" aspect-ratio="1">
              <grid-axes xlabel="x" ylabel="y"/>

              <group at="curve">
                <parametric-curve function="c" domain="(0,2*pi)" stroke="blue"
                                  thickness="3" arrows="1"
                                  arrow-location="0.35"/>
              </group>

              <group at="quarters">
                <point p="c(0)" size="4" stroke="red" fill="red"/>
                <label anchor="c(0)" alignment="ne" offset="(6,4)"
                       clear-background="yes">
                  <m>t=0</m>
                </label>
                <point p="c(pi/2)" size="4" stroke="red" fill="red"/>
                <label anchor="c(pi/2)" alignment="n" offset="(0,8)"
                       clear-background="yes">
                  <m>t=\pi/2</m>
                </label>
                <point p="c(pi)" size="4" stroke="red" fill="red"/>
                <label anchor="c(pi)" alignment="w" offset="(-8,0)"
                       clear-background="yes">
                  <m>t=\pi</m>
                </label>
                <point p="c(3*pi/2)" size="4" stroke="red" fill="red"/>
                <label anchor="c(3*pi/2)" alignment="s" offset="(0,-8)"
                       clear-background="yes">
                  <m>t=3\pi/2</m>
                </label>
              </group>

              <group at="generic">
                <line endpoints="((0,0), c(t0))" stroke="#CC79A7"
                      thickness="2"/>
                <point p="c(t0)" size="4.5" stroke="#CC79A7" fill="#CC79A7"/>
                <label anchor="c(t0)" alignment="ne" offset="(6,6)"
                       color="#CC79A7" clear-background="yes">
                  <m>\left(\cos t_0,\ \sin t_0\right)</m>
                </label>
                <label anchor="0.55*c(t0)" alignment="se" offset="(8,-6)"
                       color="#CC79A7" clear-background="yes">
                  <m>t_0</m>
                </label>
              </group>
            </coordinates>

            <annotations>
              <annotation ref="figure"
                          text="The unit circle parametrized by cosine t and
                                sine t, traced counterclockwise as t runs from
                                zero to two pi">
                <annotation ref="grid" text="The coordinate grid"/>
                <annotation ref="axes" text="The coordinate axes"/>
                <annotation ref="curve"
                            text="The unit circle, traced counterclockwise"
                            sonify="yes"/>
                <annotation ref="quarters"
                            text="The four points reached at t equals zero, pi
                                  over two, pi, and three pi over two"/>
                <annotation ref="generic"
                            text="A generic point of the circle, at parameter
                                  value t nought, joined to the origin"/>
              </annotation>
            </annotations>
          </diagram>
        </prefigure>
      </image>
    </figure>

    <exercise xml:id="check-circle-other-parametrization">
      <title>Question</title>
      <statement>
        <p>
          Can you think of another way to write a parametric form of the
          circle <m>x^2 + y^2 = 1</m>?
        </p>
      </statement>
      <hint>
        <p>
          Nothing forces the point to start at <m>(1,0)</m>, to travel
          counterclockwise, or to go around exactly once.
        </p>
      </hint>
      <answer>
        <p>
          For instance <m>x = \sin(t)</m>, <m>y = \cos(t)</m>, or
          <m>x = \cos(2t)</m>, <m>y = \sin(2t)</m>.
        </p>
      </answer>
      <solution>
        <p>
          Taking <m>x = \sin(t)</m> and <m>y = \cos(t)</m> with
          <m>t \in [0,2\pi)</m> gives the same circle, but starting at
          <m>(0,1)</m> and traced <em>clockwise</em>. Taking
          <m>x = \cos(2t)</m> and <m>y = \sin(2t)</m> with
          <m>t \in [0,2\pi)</m> also gives the same circle, traced
          counterclockwise but <em>twice</em>.
        </p>
      </solution>
    </exercise>
  </subsection>

  <!-- ================================================================= -->
  <subsection xml:id="subsec-parametrizing-examples">
    <title>Some Examples of Parametrizing Curves</title>

    <example xml:id="ex-parametrizing-curves">
      <title>Parametrizing Curves</title>
      <statement>
        <p>
          Find the parametric equations corresponding to the following
          curves.
        </p>
        <ol>
          <li>
            <p>
              The line segment connecting the two points <m>(1,0)</m> and
              <m>(0,1)</m>.
            </p>
          </li>
          <li>
            <p>
              The ellipse <m>\dfrac{x^2}{4} + \dfrac{y^2}{9} = 1</m>.
            </p>
          </li>
          <li>
            <p>
              The circle of radius <m>2</m> centered at <m>(2,0)</m>.
            </p>
          </li>
        </ol>
      </statement>

      <solution>
        <p>
          <term>Part A.</term> The segment lies on the line
          <m>y = 1 - x</m>, so we may take

        <md>
          <mrow xml:id="eq-segment-param" number="yes">
            x = t, \qquad y = 1 - t, \qquad 0 \le t \le 1.
          </mrow>
        </md>
        </p>
        <p>
          At <m>t = 0</m> we are at the point <m>(0,1)</m>, and at
          <m>t = 1</m> we arrive at the point <m>(1,0)</m>. See
          <xref ref="fig-segment-param"/>.
        </p>

        <figure xml:id="fig-segment-param">
          <caption>
            Part A: the segment <m>x = t</m>, <m>y = 1-t</m>,
            <m>0 \le t \le 1</m>, which starts at <m>(0,1)</m> when
            <m>t=0</m> and ends at <m>(1,0)</m> when <m>t=1</m>.
          </caption>
          <image xml:id="image-segment-param" width="55%">
            <shortdescription>
              The straight segment from the point (0,1) down to the point
              (1,0), with an arrow pointing from the first to the second.
            </shortdescription>
            <description>
              <p>
                A straight segment on a coordinate grid joining the point
                zero comma one on the y axis to the point one comma zero on
                the x axis. Both endpoints are marked with dots and labeled
                with the parameter values t equals zero and t equals one, and
                an arrowhead on the segment points from the first toward the
                second.
              </p>
            </description>
            <prefigure xmlns="https://prefigure.org" label="segment-param">
              <diagram dimensions="(400,340)" margins="5">
                <definition>s(t) = (t, 1-t)</definition>
                <coordinates bbox="(-0.45,-0.45,1.79,1.45)" aspect-ratio="1">
                  <grid-axes xlabel="x" ylabel="y"/>

                  <group at="curve">
                    <parametric-curve function="s" domain="(0,1)"
                                      stroke="blue" thickness="3"
                                      arrows="1" arrow-location="0.55"/>
                  </group>

                  <group at="ends">
                    <point p="s(0)" size="4.5" stroke="red" fill="red"/>
                    <label anchor="s(0)" alignment="ne" offset="(6,6)"
                           clear-background="yes">
                      <m>(0,1),\ t=0</m>
                    </label>
                    <point p="s(1)" size="4.5" stroke="red" fill="red"/>
                    <label anchor="s(1)" alignment="ne" offset="(6,6)"
                           clear-background="yes">
                      <m>(1,0),\ t=1</m>
                    </label>
                  </group>
                </coordinates>

                <annotations>
                  <annotation ref="figure"
                              text="The line segment from the point zero comma
                                    one to the point one comma zero, traced as
                                    t runs from zero to one">
                    <annotation ref="grid" text="The coordinate grid"/>
                    <annotation ref="axes" text="The coordinate axes"/>
                    <annotation ref="curve"
                                text="The segment, traced from zero comma one
                                      toward one comma zero"
                                sonify="yes"/>
                    <annotation ref="ends"
                                text="The endpoints of the segment, reached at
                                      t equals zero and t equals one"/>
                  </annotation>
                </annotations>
              </diagram>
            </prefigure>
          </image>
        </figure>

        <p>
          <term>Part B.</term> Guided by the unit circle
          <xref ref="eq-unit-circle-param"/>, we take

        <md>
          <mrow xml:id="eq-ellipse-param" number="yes">
            x = 2\cos(t), \qquad y = 3\sin(t), \qquad 0 \le t \lt 2\pi.
          </mrow>
        </md>
        </p>
        <p>
          To verify, note that

        <md>
          \frac{\left(2\cos t\right)^2}{4}
          + \frac{\left(3\sin t\right)^2}{9}
          = \cos^2 t + \sin^2 t = 1 .
        </md>
        </p>
        <p>
          See <xref ref="fig-ellipse-param"/>.
        </p>

        <figure xml:id="fig-ellipse-param">
          <caption>
            Part B: the ellipse <m>x = 2\cos(t)</m>, <m>y = 3\sin(t)</m>,
            <m>0 \le t \lt 2\pi</m>, traced counterclockwise.
          </caption>
          <image xml:id="image-ellipse-param" width="55%">
            <shortdescription>
              An ellipse taller than it is wide, traced counterclockwise, with
              its four axis points marked.
            </shortdescription>
            <description>
              <p>
                An ellipse on a coordinate grid, two units wide in the x
                direction and three units tall in the y direction, centered at
                the origin. Four dots mark the points reached at t equals
                zero, at two comma zero; t equals pi over two, at zero comma
                three; t equals pi, at minus two comma zero; and t equals
                three pi over two, at zero comma minus three. An arrowhead
                shows that the ellipse is traced counterclockwise.
              </p>
            </description>
            <prefigure xmlns="https://prefigure.org" label="ellipse-param">
              <diagram dimensions="(340,420)" margins="5">
                <definition>e(t) = (2*cos(t), 3*sin(t))</definition>
                <coordinates bbox="(-3.2,-4.2,3.2,4.2)" aspect-ratio="1">
                  <grid-axes xlabel="x" ylabel="y"/>

                  <group at="curve">
                    <parametric-curve function="e" domain="(0,2*pi)"
                                      stroke="blue" thickness="3"
                                      arrows="1" arrow-location="0.35"/>
                  </group>

                  <group at="quarters">
                    <point p="e(0)" size="4" stroke="red" fill="red"/>
                    <label anchor="e(0)" alignment="ne" offset="(6,4)"
                           clear-background="yes">
                      <m>t=0</m>
                    </label>
                    <point p="e(pi/2)" size="4" stroke="red" fill="red"/>
                    <label anchor="e(pi/2)" alignment="n" offset="(0,8)"
                           clear-background="yes">
                      <m>t=\pi/2</m>
                    </label>
                    <point p="e(pi)" size="4" stroke="red" fill="red"/>
                    <label anchor="e(pi)" alignment="w" offset="(-8,0)"
                           clear-background="yes">
                      <m>t=\pi</m>
                    </label>
                    <point p="e(3*pi/2)" size="4" stroke="red" fill="red"/>
                    <label anchor="e(3*pi/2)" alignment="s" offset="(0,-8)"
                           clear-background="yes">
                      <m>t=3\pi/2</m>
                    </label>
                  </group>
                </coordinates>

                <annotations>
                  <annotation ref="figure"
                              text="The ellipse x squared over four plus y
                                    squared over nine equals one, parametrized
                                    by two cosine t and three sine t">
                    <annotation ref="grid" text="The coordinate grid"/>
                    <annotation ref="axes" text="The coordinate axes"/>
                    <annotation ref="curve"
                                text="The ellipse, traced counterclockwise"
                                sonify="yes"/>
                    <annotation ref="quarters"
                                text="The four points reached at t equals
                                      zero, pi over two, pi, and three pi over
                                      two"/>
                  </annotation>
                </annotations>
              </diagram>
            </prefigure>
          </image>
        </figure>

        <p>
          <term>Part C.</term> The equation of such a circle in Cartesian
          coordinates is <m>(x-2)^2 + y^2 = 4</m>, so we shift the standard
          parametrization of a circle of radius <m>2</m> by <m>2</m> units in
          the <m>x</m>-direction:

        <md>
          <mrow xml:id="eq-shifted-circle-param" number="yes">
            x = 2\cos(t) + 2, \qquad y = 2\sin(t), \qquad
            0 \le t \lt 2\pi.
          </mrow>
        </md>
        </p>
        <p>
          To verify, note that

        <md>
          <mrow>
            \bigl(\left(2\cos t + 2\right) - 2\bigr)^2
            + \left(2\sin t\right)^2 \amp = 4\cos^2 t + 4\sin^2 t
          </mrow>
          <mrow>
            \amp = 4 .
          </mrow>
        </md>
        </p>
        <p>
          See <xref ref="fig-shifted-circle-param"/>.
        </p>

        <figure xml:id="fig-shifted-circle-param">
          <caption>
            Part C: the circle <m>x = 2\cos(t)+2</m>, <m>y = 2\sin(t)</m>,
            <m>0 \le t \lt 2\pi</m>, of radius <m>2</m> centered at
            <m>(2,0)</m>.
          </caption>
          <image xml:id="image-shifted-circle-param" width="55%">
            <shortdescription>
              A circle of radius two centered at the point (2,0), passing
              through the origin, traced counterclockwise.
            </shortdescription>
            <description>
              <p>
                A circle of radius two on a coordinate grid, centered at the
                point two comma zero, which is marked with a cross. The circle
                passes through the origin on the left and through the point
                four comma zero on the right. A dot marks the starting point
                four comma zero, reached at t equals zero, and an arrowhead
                shows that the circle is traced counterclockwise.
              </p>
            </description>
            <prefigure xmlns="https://prefigure.org"
                       label="shifted-circle-param">
              <diagram dimensions="(400,320)" margins="5">
                <definition>k(t) = (2*cos(t)+2, 2*sin(t))</definition>
                <coordinates bbox="(-1.2,-2.8,5.2,2.8)" aspect-ratio="1">
                  <grid-axes xlabel="x" ylabel="y"/>

                  <group at="curve">
                    <parametric-curve function="k" domain="(0,2*pi)"
                                      stroke="blue" thickness="3"
                                      arrows="1" arrow-location="0.35"/>
                  </group>

                  <group at="marks">
                    <point p="(2,0)" size="4" stroke="black" fill="black"/>
                    <label anchor="(2,0)" alignment="sw" offset="(-6,-6)"
                           clear-background="yes">
                      <m>(2,0)</m>
                    </label>
                    <point p="k(0)" size="4.5" stroke="red" fill="red"/>
                    <label anchor="k(0)" alignment="ne" offset="(6,4)"
                           clear-background="yes">
                      <m>t=0</m>
                    </label>
                  </group>
                </coordinates>

                <annotations>
                  <annotation ref="figure"
                              text="The circle of radius two centered at the
                                    point two comma zero, parametrized by two
                                    cosine t plus two and two sine t">
                    <annotation ref="grid" text="The coordinate grid"/>
                    <annotation ref="axes" text="The coordinate axes"/>
                    <annotation ref="curve"
                                text="The circle, traced counterclockwise"
                                sonify="yes"/>
                    <annotation ref="marks"
                                text="The center of the circle and its
                                      starting point, reached at t equals
                                      zero"/>
                  </annotation>
                </annotations>
              </diagram>
            </prefigure>
          </image>
        </figure>
      </solution>
    </example>
  </subsection>

  <!-- ================================================================= -->
  <subsection xml:id="subsec-intersection-collision">
    <title>Intersection Points versus Collision Points</title>

    <p>
      A parametrization carries more information than the curve it traces: it
      also records <em>when</em> the moving point visits each place on the
      curve. Because of this, there are two different questions we can ask
      about two parametric curves

    <md>
      <mrow>
        \mathbf{r}_1(t) \amp = \left\langle f_1(t),\, g_1(t)\right\rangle,
      </mrow>
      <mrow>
        \mathbf{r}_2(t) \amp = \left\langle f_2(t),\, g_2(t)\right\rangle,
      </mrow>
    </md>
    </p>

    <p>
      thought of as the trajectories of two particles.
    </p>

    <definition xml:id="def-intersection-collision">
      <title>Intersection Points and Collision Points</title>
      <statement>
        <p>
          A point <m>P</m> is an <term>intersection point</term> of the two
          curves if <m>P</m> lies on both curves; that is, if there are
          parameter values <m>t</m> and <m>s</m>, not necessarily equal, with

        <md>
          \mathbf{r}_1(t) = \overrightarrow{OP} = \mathbf{r}_2(s).
        </md>
        </p>
        <p>
          A point <m>P</m> is a <term>collision point</term> of the two
          particles if both particles are at <m>P</m> at the same time; that
          is, if there is a single value of <m>t</m> with

        <md>
          \mathbf{r}_1(t) = \mathbf{r}_2(t) = \overrightarrow{OP}.
        </md>
        </p>
      </statement>
    </definition>

    <p>
      An intersection point is a statement about the two <em>paths</em>: they
      cross, like two roads on a map. A collision point is a statement about
      the two <em>motions</em>: the cars are at the crossroads at the same
      instant.
    </p>

    <exercise xml:id="check-collision-vs-intersection">
      <title>True or False</title>
      <statement>
        <p>
          Decide whether each statement is true or false.
        </p>
        <ol marker="(a)">
          <li>
            <p>
              Every collision point of two particles is an intersection
              point of their paths.
            </p>
          </li>
          <li>
            <p>
              Every intersection point of two paths is a collision point of
              the particles that travel along them.
            </p>
          </li>
        </ol>
      </statement>
      <answer>
        <p>
          (a) True. (b) False.
        </p>
      </answer>
      <solution>
        <p>
          (a) True. If the two particles collide at <m>P</m>, then certainly
          <m>P</m> lies on both paths, so <m>P</m> is an intersection point.
        </p>
        <p>
          (b) False. The paths may cross at a point that the two particles
          visit at different times, as the next two examples show. Changing
          the parametrization of a curve does not move the curve, but it can
          create or destroy collisions.
        </p>
      </solution>
    </exercise>

    <exercise xml:id="check-intersection-collision-equations">
      <title>True or False</title>
      <statement>
        <p>
          In practice the distinction dictates how we set up the equations.
          Decide whether each statement is true or false.
        </p>
        <ol marker="(a)">
          <li>
            <p>
              To find the <term>intersection points</term> of the curves
              <m>\mathbf{r}_1(t) = \left\langle f_1(t), g_1(t)\right\rangle</m>
              and
              <m>\mathbf{r}_2(t) = \left\langle f_2(t), g_2(t)\right\rangle</m>,
              give the two curves different parameter names and solve
              <m>f_1(t) = f_2(s)</m>, <m>g_1(t) = g_2(s)</m> for the pair
              <m>(t,s)</m>.
            </p>
          </li>
          <li>
            <p>
              To find the <term>collision points</term> of the two
              particles, eliminate the parameters and intersect the two
              Cartesian curves.
            </p>
          </li>
        </ol>
      </statement>
      <answer>
        <p>
          (a) True. (b) False.
        </p>
      </answer>
      <solution>
        <p>
          (a) True. An intersection point only has to be visited by both
          curves, possibly at different times, so the two parameters are
          independent of each other. Alternatively, eliminate the parameters
          and intersect the two Cartesian curves.
        </p>
        <p>
          (b) False. Eliminating the parameters forgets the schedules, so it
          finds the intersection points. A collision requires the same
          instant <m>t</m> in both curves: use the same parameter <m>t</m>
          in both and solve <m>f_1(t) = f_2(t)</m>, <m>g_1(t) = g_2(t)</m>
          for <m>t</m>.
        </p>
      </solution>
    </exercise>

    <example xml:id="ex-paths-cross-no-collision">
      <statement>
        <p>
          Two particles move in the plane with position vectors

        <md>
          <mrow>
            \mathbf{r}_1(t) \amp = \left\langle t,\; t^2\right\rangle,
          </mrow>
          <mrow>
            \mathbf{r}_2(t) \amp = \left\langle -t,\; 2-t\right\rangle,
                                   \qquad t \in \R .
          </mrow>
        </md>
        </p>
        <p>
          Find all intersection points of their paths, and all collision
          points of the particles.
        </p>
      </statement>

      <solution>
        <p>
          <term>Paths.</term> Eliminating the parameter, the first particle
          traces the parabola <m>y = x^2</m>. For the second, <m>x = -t</m>
          and <m>y = 2-t = 2+x</m>, so it traces the line <m>y = x+2</m>.
          Setting <m>x^2 = x+2</m> gives
          <m>x^2 - x - 2 = (x-2)(x+1) = 0</m>, so the two paths intersect
          at the two points

        <men xml:id="eq-parabola-line-intersections">
          (-1,1) \qquad \text{and} \qquad (2,4).
        </men>
        </p>
        <p>
          <term>Collisions.</term> A collision requires a single <m>t</m>
          with

        <md>
          t = -t \qquad \text{and} \qquad t^2 = 2 - t .
        </md>
        </p>
        <p>
          The first equation forces <m>t = 0</m>, but then <m>t^2 = 0</m>
          while <m>2 - t = 2</m>. There is no solution, so the particles
          never collide.
        </p>
        <p>
          We can see exactly how they miss each other. The first particle is
          at <m>(-1,1)</m> when <m>t=-1</m>, while the second particle
          arrives there only at <m>t=1</m>. Similarly, the first particle
          reaches <m>(2,4)</m> at <m>t=2</m>, long after the second particle
          passed through it at <m>t=-2</m>. Each intersection point is
          visited twice, but never simultaneously. See
          <xref ref="fig-intersection-no-collision"/>.
        </p>

        <figure xml:id="fig-intersection-no-collision">
          <caption>
            The parabola <m>C_1</m> and the line <m>C_2</m> intersect at
            <m>(-1,1)</m> and <m>(2,4)</m>. At each of these points the two
            particles arrive at different times, so neither point is a
            collision point.
          </caption>
          <image xml:id="image-intersection-no-collision" width="75%">
            <shortdescription>
              A parabola and a line crossing at two points, each labeled with
              the two different times at which the particles pass through it.
            </shortdescription>
            <description>
              <p>
                The parabola y equals x squared and the line y equals x plus
                two drawn on the same coordinate grid, crossing at the points
                minus one comma one and two comma four. Each crossing is
                marked with a dot and labeled with two different parameter
                values: at minus one comma one the first particle arrives at t
                equals minus one and the second at t equals one, and at two
                comma four the first arrives at t equals two and the second at
                t equals minus two.
              </p>
            </description>
            <prefigure xmlns="https://prefigure.org"
                       label="intersection-no-collision">
              <diagram dimensions="(400,400)" margins="5">
                <definition>p(t) = (t, t^2)</definition>
                <definition>l(t) = (-t, 2-t)</definition>
                <coordinates bbox="(-4.6,-1.8,5.6,7.2)">
                  <grid-axes xlabel="x" ylabel="y"/>

                  <group at="parabola">
                    <!-- the arrow sits well up the right branch: at
                         t=2 it would land under the crossing point -->
                    <parametric-curve function="p" domain="(-2.4,2.6)"
                                      stroke="blue" thickness="3"
                                      arrows="1" arrow-location="2.45"/>
                    <label anchor="p(-2.3)" alignment="nw" offset="(-6,4)"
                           color="blue">
                      <m>C_1</m>
                    </label>
                  </group>

                  <group at="line">
                    <parametric-curve function="l" domain="(-3.4,3.1)"
                                      stroke="#009E73" thickness="3"
                                      arrows="1" arrow-location="2"/>
                    <label anchor="l(-2.9)" alignment="se" offset="(8,-2)"
                           color="#009E73">
                      <m>C_2</m>
                    </label>
                  </group>

                  <!-- Each crossing is annotated in the empty wedge between
                       the two curves, with a thin leader back to the point,
                       so that no label sits on a curve or runs off the
                       edge of the diagram. -->
                  <group at="crossings">
                    <point p="(-1,1)" size="4.5" stroke="red" fill="red"/>
                    <line endpoints="((-1.15,1.05), (-2.2,1.5))"
                          stroke="gray" thickness="1"/>
                    <label anchor="(-3.1,3.1)" alignment="c">
                      <m>(-1,1)</m>
                    </label>
                    <label anchor="(-3.1,2.3)" alignment="c">
                      <m>C_1: t=-1</m>
                    </label>
                    <label anchor="(-3.1,1.5)" alignment="c">
                      <m>C_2: t=1</m>
                    </label>
                    <point p="(2,4)" size="4.5" stroke="red" fill="red"/>
                    <line endpoints="((2.15,3.95), (3.05,3.7))"
                          stroke="gray" thickness="1"/>
                    <label anchor="(3.85,3.7)" alignment="c">
                      <m>(2,4)</m>
                    </label>
                    <label anchor="(3.85,2.9)" alignment="c">
                      <m>C_1: t=2</m>
                    </label>
                    <label anchor="(3.85,2.1)" alignment="c">
                      <m>C_2: t=-2</m>
                    </label>
                  </group>
                </coordinates>

                <annotations>
                  <annotation ref="figure"
                              text="A parabola and a line crossing at two
                                    points that the two particles reach at
                                    different times, so there is no collision">
                    <annotation ref="grid" text="The coordinate grid"/>
                    <annotation ref="axes" text="The coordinate axes"/>
                    <annotation ref="parabola"
                                text="The path of the first particle, the
                                      parabola y equals x squared"
                                sonify="yes"/>
                    <annotation ref="line"
                                text="The path of the second particle, the
                                      line y equals x plus two"
                                sonify="yes"/>
                    <annotation ref="crossings"
                                text="The two intersection points, each
                                      labeled with the two different times at
                                      which the particles pass through it"/>
                  </annotation>
                </annotations>
              </diagram>
            </prefigure>
          </image>
        </figure>
      </solution>
    </example>

    <example xml:id="ex-same-paths-collision">
      <statement>
        <p>
          Keep the first particle, but re-schedule the second one:

        <md>
          <mrow>
            \mathbf{r}_1(t) \amp = \left\langle t,\; t^2\right\rangle,
          </mrow>
          <mrow>
            \mathbf{r}_2(t) \amp = \left\langle 2t-2,\; 2t\right\rangle,
                                   \qquad t \in \R .
          </mrow>
        </md>
        </p>
        <p>
          Find the intersection points of the paths and the collision points
          of the particles.
        </p>
      </statement>

      <solution>
        <p>
          <term>Paths.</term> For the second particle, <m>x = 2t-2</m> and
          <m>y = 2t = x+2</m>, so it travels along the very same line
          <m>y = x+2</m> as before, only faster and in the opposite
          direction. The paths are therefore unchanged, and they still
          intersect at the points <xref ref="eq-parabola-line-intersections"/>,
          <m>(-1,1)</m> and <m>(2,4)</m>.
        </p>
        <p>
          <term>Collisions.</term> Now a collision requires a single <m>t</m>
          with

        <md>
          t = 2t - 2 \qquad \text{and} \qquad t^2 = 2t .
        </md>
        </p>
        <p>
          The first equation gives <m>t = 2</m>, and this value does satisfy
          the second equation, since <m>2^2 = 4 = 2\cdot 2</m>. Hence the
          particles do collide, at

        <md>
          \mathbf{r}_1(2) = \mathbf{r}_2(2) = \left\langle 2,4\right\rangle, \qquad
          \text{at time } t = 2 .
        </md>
        </p>
        <p>
          The other intersection point is not a collision point: the first
          particle passes through <m>(-1,1)</m> at <m>t=-1</m>, while the
          second particle passes through it at <m>t = \tfrac12</m>. Two
          intersection points, but only one collision. See
          <xref ref="fig-collision"/>.
        </p>

        <figure xml:id="fig-collision">
          <caption>
            The same two paths as in
            <xref ref="fig-intersection-no-collision"/>. The point
            <m>(2,4)</m> is now a collision point, reached by both particles
            at <m>t=2</m>, while <m>(-1,1)</m> remains an intersection point
            that is not a collision point.
          </caption>
          <image xml:id="image-collision" width="75%">
            <shortdescription>
              The same parabola and line, with the crossing at (2,4)
              highlighted as a collision point reached by both particles at
              time t equals two.
            </shortdescription>
            <description>
              <p>
                The parabola y equals x squared and the line y equals x plus
                two crossing at minus one comma one and at two comma four. The
                point two comma four is drawn as a large highlighted dot and
                labeled as a collision point, reached by both particles at
                time t equals two, while the particles pass through minus one
                comma one at the different times t equals minus one and t
                equals one half.
              </p>
            </description>
            <prefigure xmlns="https://prefigure.org" label="collision">
              <diagram dimensions="(400,400)" margins="5">
                <definition>p(t) = (t, t^2)</definition>
                <definition>l(t) = (t, t+2)</definition>
                <coordinates bbox="(-4.6,-1.8,5.6,7.2)">
                  <grid-axes xlabel="x" ylabel="y"/>

                  <group at="parabola">
                    <!-- both arrows are placed before t=2, so they show
                         the two particles running up to the collision -->
                    <parametric-curve function="p" domain="(-2.4,2.6)"
                                      stroke="blue" thickness="3"
                                      arrows="1" arrow-location="1.5"/>
                    <label anchor="p(-2.3)" alignment="nw" offset="(-6,4)"
                           color="blue">
                      <m>C_1</m>
                    </label>
                  </group>

                  <group at="line">
                    <parametric-curve function="l" domain="(-3.1,3.4)"
                                      stroke="#009E73" thickness="3"
                                      arrows="1" arrow-location="1.15"/>
                    <label anchor="l(3.0)" alignment="se" offset="(8,-2)"
                           color="#009E73">
                      <m>C_2</m>
                    </label>
                  </group>

                  <!-- as in the previous figure, the annotations sit in the
                       empty wedges with a leader back to the crossing -->
                  <group at="near-miss">
                    <point p="(-1,1)" size="4.5" stroke="red" fill="red"/>
                    <line endpoints="((-1.15,1.05), (-2.2,1.5))"
                          stroke="gray" thickness="1"/>
                    <label anchor="(-3.1,3.1)" alignment="c">
                      <m>(-1,1)</m>
                    </label>
                    <label anchor="(-3.1,2.3)" alignment="c">
                      <m>C_1: t=-1</m>
                    </label>
                    <label anchor="(-3.1,1.5)" alignment="c">
                      <m>C_2: t=\tfrac12</m>
                    </label>
                  </group>

                  <group at="collision">
                    <circle center="(2,4)" radius="0.32" stroke="#D55E00"
                            thickness="3"/>
                    <point p="(2,4)" size="5.5" stroke="#D55E00"
                           fill="#D55E00"/>
                    <line endpoints="((2.35,3.9), (3.05,3.7))"
                          stroke="gray" thickness="1"/>
                    <label anchor="(3.9,3.7)" alignment="c" color="#D55E00">
                      <m>(2,4)</m>
                    </label>
                    <label anchor="(3.9,2.9)" alignment="c" color="#D55E00">
                      <m>\text{collision}</m>
                    </label>
                    <label anchor="(3.9,2.1)" alignment="c" color="#D55E00">
                      <m>\text{at } t=2</m>
                    </label>
                  </group>
                </coordinates>

                <annotations>
                  <annotation ref="figure"
                              text="The same parabola and line, with the
                                    crossing at two comma four highlighted as
                                    a collision point">
                    <annotation ref="grid" text="The coordinate grid"/>
                    <annotation ref="axes" text="The coordinate axes"/>
                    <annotation ref="parabola"
                                text="The path of the first particle, the
                                      parabola y equals x squared"
                                sonify="yes"/>
                    <annotation ref="line"
                                text="The path of the second particle, the
                                      line y equals x plus two"
                                sonify="yes"/>
                    <annotation ref="near-miss"
                                text="The intersection point minus one comma
                                      one, which the particles reach at the
                                      different times t equals minus one and t
                                      equals one half"/>
                    <annotation ref="collision"
                                text="The collision point two comma four,
                                      which both particles reach at time t
                                      equals two"/>
                  </annotation>
                </annotations>
              </diagram>
            </prefigure>
          </image>
        </figure>
      </solution>
    </example>

    <exercise xml:id="check-collision-both-points">
      <statement>
        <p>
          Keep <m>\mathbf{r}_1(t) = \left\langle t,t^2\right\rangle</m>. Find a parametrization
          <m>\mathbf{r}_2</m> of the line <m>y = x+2</m> for which the two
          particles collide at <em>both</em> intersection points.
        </p>
      </statement>
      <answer>
        <p>
          <m>\mathbf{r}_2(t) = \left\langle t,\; t+2\right\rangle</m>.
        </p>
      </answer>
      <solution>
        <p>
          With <m>\mathbf{r}_2(t) = \left\langle t, t+2\right\rangle</m> the collision equations
          become <m>t = t</m> and <m>t^2 = t+2</m>, that is,
          <m>(t-2)(t+1) = 0</m>. Thus <m>t = -1</m> gives the collision at
          <m>(-1,1)</m> and <m>t = 2</m> gives the collision at
          <m>(2,4)</m>: both intersection points are now collision points.
        </p>
      </solution>
    </exercise>
  </subsection>

  <!-- ================================================================= -->
  <subsection xml:id="subsec-curves-in-space">
    <title>Curves in Space and Vector Functions</title>

    <p>
      Assume that we are interested in describing the path of an object in
      space. To this aim, we can specify its location at each instant of
      time; that is, we can write its coordinates in <m>\R^3</m> as

    <md>
      <mrow xml:id="eq-space-parametric" number="yes">
        x = f(t), \qquad y = g(t), \qquad z = h(t).
      </mrow>
    </md>
    </p>

    <p>
      Here <m>t</m> is the time parameter, which varies from some initial
      time <m>t_i</m> to some final time <m>t_f</m>, that is,
      <m>t \in [t_i, t_f]</m>. Nothing has changed, except that where a
      plane curve needed the two equations
      <xref ref="eq-parametric-equations"/>, a space curve needs three.
    </p>

    <p>
      There is a second, more compact way to look at
      <xref ref="eq-space-parametric"/>, and it is the point of view we will
      use for the rest of the course. Instead of three separate coordinate
      equations, collect the coordinates into a single
      <term>vector function</term> <mdash/> a function whose input is a real
      number and whose output is a vector:

    <md>
      <mrow>
        \mathbf{r}(t) \amp = \left\langle f(t),\ g(t),\ h(t)
                             \right\rangle
      </mrow>
      <mrow xml:id="eq-vector-function" number="yes">
        \amp = f(t)\,\mathbf{i} + g(t)\,\mathbf{j} + h(t)\,\mathbf{k},
               \qquad t \in [t_i,t_f].
      </mrow>
    </md>
    </p>

    <p>
      Think of <m>\mathbf{r}(t)</m> as the <term>position vector</term> of
      the moving object: an arrow drawn from the origin to the object's
      location at time <m>t</m>. As <m>t</m> increases, the arrow swings
      around and changes length, and its <em>tip</em> sweeps out the curve.
      So a parametrization can be read in two equivalent ways.
    </p>

    <ul>
      <li>
        <p>
          <term>Coordinatewise.</term> Three ordinary scalar functions
          <m>f</m>, <m>g</m>, <m>h</m> of one variable, one for each
          coordinate, as in <xref ref="eq-space-parametric"/>.
        </p>
      </li>
      <li>
        <p>
          <term>Vectorially.</term> One vector-valued function
          <m>\mathbf{r}</m> of one variable, as in
          <xref ref="eq-vector-function"/>. The curve is the set of tips of
          the vectors <m>\mathbf{r}(t)</m>.
        </p>
      </li>
    </ul>

    <p>
      The two descriptions carry exactly the same information, but the vector
      form is the more useful one: it lets us differentiate and integrate a
      whole motion at once, and its derivative will turn out to be the
      velocity of the moving object. The same device works in the plane,
      where <m>\mathbf{r}(t) = \left\langle f(t), g(t)\right\rangle</m>; the
      position vectors <m>\mathbf{r}_1</m> and <m>\mathbf{r}_2</m> of
      <xref ref="ex-paths-cross-no-collision"/> were already written this
      way.
    </p>

    <example xml:id="ex-helix-space-curve">
      <title>A helix</title>
      <statement>
        <p>
          Consider the object whose coordinates are parametrized by

        <md>
          <mrow>
            x \amp = \cos(2t), \qquad y = \sin(2t),
          </mrow>
          <mrow xml:id="eq-helix-param-space" number="yes">
            z \amp = t, \qquad t \in [0,\pi].
          </mrow>
        </md>
        </p>
        <p>
          Write the motion as a vector function and describe the path.
        </p>
      </statement>

      <solution>
        <p>
          Collecting <xref ref="eq-helix-param-space"/> into a single vector
          function,

        <md>
          \mathbf{r}(t) = \left\langle \cos(2t),\ \sin(2t),\ t
          \right\rangle , \qquad 0 \le t \le \pi .
        </md>
        </p>
        <p>
          The first two coordinates satisfy <m>x^2 + y^2 = 1</m>, so the
          shadow of the path on the <m>xy</m>-plane is the unit circle, and
          as <m>t</m> runs from <m>0</m> to <m>\pi</m> the angle <m>2t</m>
          runs from <m>0</m> to <m>2\pi</m>: the shadow goes around exactly
          once. Meanwhile <m>z = t</m> increases steadily, so the object
          climbs at a constant rate while circling. The path is therefore one
          full turn of a <term>helix</term>, shown in
          <xref ref="fig-helix-space-curve"/>. The motion begins at
          <m>\mathbf{r}(0) = \left\langle 1,0,0\right\rangle</m> at time <m>t=0</m> and ends at
          <m>\mathbf{r}(\pi) = \left\langle 1,0,\pi\right\rangle</m> at time <m>t=\pi</m>, directly
          above the starting point.
        </p>

        <figure xml:id="fig-helix-space-curve">
          <caption>
            One turn of the helix
            <m>\mathbf{r}(t) = \left\langle \cos(2t), \sin(2t), t
            \right\rangle</m>, <m>0 \le t \le \pi</m>. The violet arrow is
            the position vector <m>\mathbf{r}(t)</m> at
            <m>t = 3\pi/8</m>; its tip rides along the curve as <m>t</m>
            increases. The dashed gray circle is the shadow of the path on
            the plane <m>z=0</m>.
          </caption>
          <image xml:id="image-helix-space-curve" width="70%">
            <shortdescription>
              One turn of a helix rising above the unit circle, with the
              position vector drawn from the origin to a point on the curve.
            </shortdescription>
            <description>
              <p>
                A three-dimensional coordinate box with x and y running from
                minus 1.3 to 1.3 and z from 0 to about 3.4. A thick blue curve
                spirals counterclockwise once around the vertical axis while
                climbing steadily, starting at a red point labeled (1,0,0) at
                time t equals zero on the floor of the box and ending at a red
                point labeled (1,0,pi) at time t equals pi, directly above the
                start. A dashed gray unit circle on the floor is the shadow of
                the path. A violet arrow labeled r of t runs from the origin
                to a point partway up the curve, illustrating the position
                vector whose tip traces the helix.
              </p>
            </description>
            <latex-image>
              \begin{tikzpicture}
              \begin{axis}[
                  width=10cm, view={115}{18},
                  xlabel={\(x\)}, ylabel={\(y\)}, zlabel={\(z\)},
                  xmin=-1.3, xmax=1.3, ymin=-1.3, ymax=1.3,
                  zmin=0, zmax=3.4,
                  grid=major, grid style={gray!20}, clip=false,
                ]
                % shadow of the path on the plane z=0
                \addplot3[gray!65, dashed, line width=1.3pt, domain=0:180,
                          samples=120, samples y=0]
                    ({cos(2*x)}, {sin(2*x)}, {0});
                % the helix itself
                \addplot3[blue, very thick, domain=0:180,
                          samples=200, samples y=0]
                    ({cos(2*x)}, {sin(2*x)}, {pi*x/180});
                % the position vector r(t) at t = 3pi/8
                \addplot3[-{Stealth[length=2.5mm]}, violet, thick]
                    coordinates {(0,0,0) (-0.70711,0.70711,1.17810)};
                \addplot3[only marks, mark=*, violet, mark size=1.8pt]
                    coordinates {(-0.70711,0.70711,1.17810)};
                % start and end of the motion
                \addplot3[only marks, mark=*, red, mark size=1.8pt]
                    coordinates {(1,0,0) (1,0,3.14159)};
                \node[left=4pt, yshift=7pt] at (axis cs:1,0,0)
                    {\((1,0,0),\ t=0\)};
                \node[above right] at (axis cs:1,0,3.14159)
                    {\((1,0,\pi),\ t=\pi\)};
                \node[violet, above left=-2pt]
                    at (axis cs:-0.35355,0.35355,0.58905)
                    {\(\mathbf{r}(t)\)};
              \end{axis}
              \end{tikzpicture}
            </latex-image>
          </image>
        </figure>
      </solution>
    </example>

    <p>
      There are situations in which a curve in space arises as the
      intersection of two surfaces. In the next two examples we turn such a
      description into a vector function <m>\mathbf{r}(t)</m>.
    </p>

    <example xml:id="ex-cylinder-plane">
      <title>Where a plane cuts a cylinder</title>
      <statement>
        <p>
          Find a vector function that represents the curve of intersection of
          the cylinder <m>x^2 + y^2 = 1</m> and the plane
          <m>y + z = 2</m>.
        </p>
      </statement>

      <solution>
        <p>
          As shown in <xref ref="fig-cylinder-plane"/>, the intersection of
          the two surfaces is an ellipse. Furthermore, its projection onto
          the <m>xy</m>-plane is the circle <m>x^2 + y^2 = 1</m>, and we
          already know its parametrization from
          <xref ref="eq-unit-circle-param"/>:

        <md>
          x(t) = \cos(t), \qquad y(t) = \sin(t).
        </md>
        </p>
        <p>
          Then we use the fact that <m>y + z = 2</m> and write <m>z</m> in
          terms of <m>t</m>:

        <md>
          z(t) = 2 - y(t) = 2 - \sin(t).
        </md>
        </p>
        <p>
          Hence the vector function representing the intersection of
          <m>x^2+y^2=1</m> and <m>y+z=2</m> is

        <md>
          <mrow xml:id="eq-cylinder-plane-param" number="yes">
            \mathbf{r}(t) = \cos(t)\,\mathbf{i} + \sin(t)\,\mathbf{j}
                            + \bigl(2 - \sin(t)\bigr)\,\mathbf{k},
          </mrow>
        </md>
        </p>
        <p>
          where <m>0 \le t \le 2\pi</m>. As a check, the four points
          <m>\mathbf{r}(0) = \left\langle 1,0,2\right\rangle</m>,
          <m>\mathbf{r}(\pi/2) = \left\langle 0,1,1\right\rangle</m>,
          <m>\mathbf{r}(\pi) = \left\langle -1,0,2\right\rangle</m> and
          <m>\mathbf{r}(3\pi/2) = \left\langle 0,-1,3\right\rangle</m> all
          satisfy both equations.
        </p>

        <figure xml:id="fig-cylinder-plane">
          <caption>
            The cylinder <m>x^2+y^2=1</m>, the plane <m>y+z=2</m>, and the
            ellipse <m>C</m> in which they meet. The violet arrow is the
            position vector <m>\mathbf{r}(t)</m> of
            <xref ref="eq-cylinder-plane-param"/> at <m>t=\pi/4</m>.
          </caption>
          <image xml:id="image-cylinder-plane" width="70%">
            <shortdescription>
              A vertical cylinder cut by a slanted plane, with the ellipse
              where they meet drawn on the surfaces.
            </shortdescription>
            <description>
              <p>
                A three-dimensional plot showing a vertical circular cylinder
                of radius one standing on the xy-plane, and a slanted plane
                cutting through it. The two meet along a closed curve, an
                ellipse, drawn as a thick magenta loop and labeled C. Four
                points on the ellipse are marked, at (1,0,2), (0,1,1),
                (-1,0,2) and (0,-1,3). A violet arrow runs from the origin to
                a point on the ellipse, illustrating the position vector r of
                t.
              </p>
            </description>
            <latex-image>
              \begin{tikzpicture}
              \begin{axis}[
                  width=10.5cm, view={120}{16},
                  xlabel={\(x\)}, ylabel={\(y\)}, zlabel={\(z\)},
                  xmin=-1.9, xmax=1.9, ymin=-1.9, ymax=1.9,
                  zmin=0, zmax=3.6,
                  grid=major, grid style={gray!20}, clip=false,
                ]
                % the cylinder x^2 + y^2 = 1
                \addplot3[surf, shader=flat, opacity=0.30, draw=gray!45,
                          colormap={cylgray}{color=(gray!15) color=(gray!45)},
                          domain=0:360, y domain=0:3.2,
                          samples=48, samples y=2]
                    ({cos(x)}, {sin(x)}, {y});
                % the plane y + z = 2
                \draw[blue!55, fill=blue!25, fill opacity=0.30]
                    (axis cs:-1.9,-1.6,3.6) -- (axis cs:1.9,-1.6,3.6)
                    -- (axis cs:1.9,1.9,0.1) -- (axis cs:-1.9,1.9,0.1)
                    -- cycle;
                % the curve of intersection
                \addplot3[magenta, very thick, domain=0:360,
                          samples=180, samples y=0]
                    ({cos(x)}, {sin(x)}, {2-sin(x)});
                % the position vector r(pi/4)
                \addplot3[-{Stealth[length=2.5mm]}, violet, thick]
                    coordinates {(0,0,0) (0.70711,0.70711,1.29289)};
                \addplot3[only marks, mark=*, black, mark size=1.6pt]
                    coordinates {(1,0,2) (0,1,1) (-1,0,2) (0,-1,3)};
                \node[right=1pt] at (axis cs:1,0,2) {\((1,0,2)\)};
                \node[below=1pt] at (axis cs:0,1,1) {\((0,1,1)\)};
                \node[left=1pt] at (axis cs:-1,0,2) {\((-1,0,2)\)};
                \node[above=1pt] at (axis cs:0,-1,3) {\((0,-1,3)\)};
                \node[magenta, above left=-2pt]
                    at (axis cs:-0.70711,-0.70711,2.70711) {\(C\)};
                \node[violet, above left=-2pt]
                    at (axis cs:0.35355,0.35355,0.64645)
                    {\(\mathbf{r}(t)\)};
              \end{axis}
              \end{tikzpicture}
            </latex-image>
          </image>
        </figure>
      </solution>
    </example>

    <example xml:id="ex-two-surfaces">
      <title>Where two curved surfaces meet</title>
      <statement>
        <p>
          Parametrize the intersection of the surfaces

        <md>
          z = \sqrt{1 + x^2 + y^2}
          \qquad \text{and} \qquad
          z = \sqrt{4 - 4x^2 - y^2} .
        </md>
        </p>
      </statement>

      <solution>
        <p>
          On the intersection the two expressions for <m>z</m> agree.
          Squaring both sides of

        <md>
          \sqrt{1 + x^2 + y^2} = \sqrt{4 - 4x^2 - y^2}
        </md>
        </p>
        <p>
          gives <m>1 + x^2 + y^2 = 4 - 4x^2 - y^2</m>, that is,

        <md>
          <mrow>
            5x^2 + 2y^2 \amp = 3,
          </mrow>
          <mrow xml:id="eq-two-surfaces-shadow" number="yes">
            \text{or} \quad
            \left(\frac{x}{\sqrt{3/5}}\right)^{2}
            + \left(\frac{y}{\sqrt{3/2}}\right)^{2} \amp = 1 .
          </mrow>
        </md>
        </p>
        <p>
          Notice that <m>z</m> has disappeared: equation
          <xref ref="eq-two-surfaces-shadow"/> describes the shadow of the
          intersection curve in the <m>xy</m>-plane, and it is an ellipse.
          Parametrizing it exactly as in
          <xref ref="eq-ellipse-param"/>,

        <md>
          <mrow>
            x(t) \amp = \sqrt{\tfrac{3}{5}}\,\cos(t),
          </mrow>
          <mrow>
            y(t) \amp = \sqrt{\tfrac{3}{2}}\,\sin(t), \qquad
                        0 \le t \le 2\pi .
          </mrow>
        </md>
        </p>
        <p>
          Plugging these into either of the two original equations, say
          <m>z = \sqrt{1+x^2+y^2}</m>, recovers the missing coordinate:

        <md>
          z(t) = \sqrt{1 + \tfrac{3}{5}\cos^2(t)
                         + \tfrac{3}{2}\sin^2(t)} .
        </md>
        </p>
        <p>
          Hence the curve of intersection, shown in
          <xref ref="fig-two-surfaces"/>, is

        <md>
          <mrow>
            \mathbf{r}(t) \amp = \Bigl\langle
              \sqrt{\tfrac{3}{5}}\,\cos(t),\ \
              \sqrt{\tfrac{3}{2}}\,\sin(t),
          </mrow>
          <mrow xml:id="eq-two-surfaces-param" number="yes">
            \amp \qquad\quad
              \sqrt{1 + \tfrac{3}{5}\cos^2(t) + \tfrac{3}{2}\sin^2(t)}
              \Bigr\rangle ,
          </mrow>
        </md>
        </p>
        <p>
          for <m>0 \le t \le 2\pi</m>.
        </p>

        <figure xml:id="fig-two-surfaces">
          <caption>
            The upper sheet of <m>z = \sqrt{1+x^2+y^2}</m> (blue) and the
            dome <m>z = \sqrt{4-4x^2-y^2}</m> (red) meet along the closed
            curve <xref ref="eq-two-surfaces-param"/>, drawn in black. Its
            shadow in the plane <m>z=0</m> is the dashed ellipse
            <xref ref="eq-two-surfaces-shadow"/>.
          </caption>
          <image xml:id="image-two-surfaces" width="100%">
            <shortdescription>
              A bowl-shaped surface and a dome overlapping, with the closed
              curve where they meet drawn on them.
            </shortdescription>
            <description>
              <p>
                A three-dimensional plot showing two surfaces. The first, in
                blue, is the upper sheet of a hyperboloid opening upward from
                its lowest point at height one above the origin. The second,
                in red, is a dome, the upper half of an ellipsoid, reaching
                height two above the origin. The two surfaces cross along a
                closed curve, drawn in black, which loops around the vertical
                axis at heights between about 1.26 and 1.58. A dashed ellipse
                on the floor of the box shows the shadow of that curve.
              </p>
            </description>
            <latex-image>
              \begin{tikzpicture}
              \begin{axis}[
                  width=10.5cm, view={120}{16},
                  xlabel={\(x\)}, ylabel={\(y\)}, zlabel={\(z\)},
                  xmin=-1.6, xmax=1.6, ymin=-2.2, ymax=2.2,
                  zmin=0, zmax=2.4,
                  grid=major, grid style={gray!20}, clip=false,
                  % the surfaces are named in a legend rather than on the plot
                  legend style={at={(1.02,1)}, anchor=north west,
                                draw=gray!45, fill=white, font=\small,
                                cells={anchor=west}, row sep=2pt},
                  legend cell align=left,
                ]
                % the dome z = sqrt(4 - 4x^2 - y^2)
                \addplot3[surf, shader=flat, opacity=0.50, draw=red!55,
                          colormap={domered}{color=(red!25) color=(red!55)},
                          domain=0:90, y domain=0:360,
                          samples=16, samples y=48]
                    ({sin(x)*cos(y)}, {2*sin(x)*sin(y)}, {2*cos(x)});
                % the upper sheet z = sqrt(1 + x^2 + y^2)
                \addplot3[surf, shader=flat, opacity=0.50, draw=blue!55,
                          colormap={sheetblue}{color=(blue!25) color=(blue!55)},
                          domain=0:1.5, y domain=0:360,
                          samples=14, samples y=48]
                    ({x*cos(y)}, {x*sin(y)}, {sqrt(1+x^2)});
                % shadow of the intersection curve
                \addplot3[gray!70, dashed, line width=2pt, domain=0:360,
                          samples=120, samples y=0]
                    ({0.774597*cos(x)}, {1.224745*sin(x)}, {0});
                % the curve of intersection
                \addplot3[black, very thick, domain=0:360,
                          samples=180, samples y=0]
                    ({0.774597*cos(x)}, {1.224745*sin(x)},
                     {sqrt(1+0.6*(cos(x))^2+1.5*(sin(x))^2)});
                % One entry per plot, in the order the plots were drawn:
                % dome, sheet, shadow, curve.  (An \addlegendentry binds to
                % the next plot without a label, so the order matters.)
                \legend{\(z=\sqrt{4-4x^2-y^2}\),
                        \(z=\sqrt{1+x^2+y^2}\),
                        shadow of the curve\, \(z=0\),
                        curve of intersection}
              \end{axis}
              \end{tikzpicture}
            </latex-image>
          </image>
        </figure>
      </solution>
    </example>
  </subsection>

</section>
